{
  "name": "olden",
  "version": "0.1.3",
  "main": "./app/main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron app",
    "test": "./node_modules/.bin/mocha --reporter spec",
    "build": "node scripts/build.js && npm i --only=prod --prefix ./app",
    "package_macos": "node scripts/package_macos.js",
    "package_win": "node scripts/package_windows.js",
    "bootstrap": "npm i -g bower && npm i"
  },
  "dependencies": {
    "dexie": "^1.4.1"
  },
  "optionalDependencies": {
    "appdmg": "^0.4.5"
  },
  "devDependencies": {
    "archiver": "^1.1.0",
    "electron": "^1.3.5",
    "electron-packager": "^8.0.0",
    "mocha": "^3.0.2",
    "rimraf": "^2.5.4",
    "spectron": "^3.3.0"
  }
}
