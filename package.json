{
  "name": "Olden",
  "version": "0.1.2",
  "main": "./app/main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron app",
    "test": "./node_modules/.bin/mocha --reporter spec",
    "build": "node scripts/build.js && npm install --prefix ./app",
    "package_macos": "node scripts/package_macos.js",
    "package_win": "node scripts/package_windows.js",
    "bootstrap": "npm i -g bower && npm i --only=dev"
  },
  "dependencies": {
    "dexie": "^1.4.1"
  },
  "devDependencies": {
    "appdmg": "^0.4.5",
    "archiver": "^1.1.0",
    "electron": "^1.3.5",
    "electron-packager": "^8.0.0",
    "mocha": "^3.0.2",
    "rimraf": "^2.5.4",
    "spectron": "^3.3.0"
  }
}
