{
  "name": "Olden",
  "version": "0.0.1",
  "main": "./app/main.js",
  "scripts": {
    "start": "electron app",
    "build": "node scripts/build.js && npm install --prefix ./app",
    "package": "electron-packager . --all --version=1.3.5 --app-version=0.0.1 --out=dist",
    "bootstrap": "npm install -g electron-prebuilt electron-packager"
  },
  "dependencies": {
    "dexie": "^1.4.1"
  }
}
