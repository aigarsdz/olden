{
  "name": "Olden",
  "version": "0.0.1",
  "main": "./app/main.js",
  "scripts": {
    "start": "electron app",
    "build": "node scripts/build.js && npm install --prefix ./app",
    "package_macos": "node scripts/package_macos.js",
    "package_win": "node scripts/package_windows.js",
    "bootstrap": "npm i -g electron-prebuilt && npm i --only=dev"
  },
  "dependencies": {
    "dexie": "^1.4.1"
  },
  "devDependencies": {
    "appdmg": "^0.4.5",
    "archiver": "^1.1.0",
    "electron-packager": "^8.0.0",
    "rimraf": "^2.5.4"
  }
}
